## Runic Sentinel 1.4.2

[General]
# False creates no worker, admission gate, services, UI, transition patch, or operator command.
Enabled = true

[Administrator Access]
# Server-side adminlist.txt members and local listen hosts may administer Sentinel.
# False limits access to independently signed Sentinel roles.
UseServerAdminList = true

[Policy]
# Exact canonical RUNIC-SENTINEL/3 bytes. Relative paths resolve under BepInEx/config.
ManifestFile = RunicSentinel.policy
# One canonical Base64 RSA-3072 signature followed by exactly one LF.
SignatureFile = RunicSentinel.policy.sig
# Exact canonical RUNIC-RSA-PUBLIC/1 verification-only key file.
# Public verification file. The optional F3 workflow stores its private key separately under
# BepInEx/config/RunicSentinel/server-private and never transmits or packages it.
PublicKeyFile = RunicSentinel.policy.pub
# Required lowercase SHA-256 of exact public-key bytes. Empty means monitor-only.
TrustedPublicKeySha256 =

[Remote Admission]
# Sampled at startup; restart after changing. Optional records incompatibility without delaying
# Valheim admission. Required withholds the native server handshake and PeerInfo admission until
# Runic Sentinel Client returns an accepted bounded report.
Policy = Optional

[Runtime Integrity]
CheckIntervalSeconds = 15

[Transition Safety]
BackupWorldBeforeProfileChange = true

[Automatic Enforcement]
VeryHighFindingsBeforeDisconnect = 2
HighFindingsBeforeDisconnect = 3
FindingWindowSeconds = 60

[Administrator Panel]
# The server authorizes adminlist.txt members, the local host, or signed Sentinel roles.
OpenPanel = F3
